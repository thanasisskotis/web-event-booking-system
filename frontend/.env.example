# API base URL. Use https when running the backend over TLS for the demo.
VITE_API_URL=http://localhost:8000

# Set to "true" to serve the Vite dev server over HTTPS.
# Requires certs at ../certs (run scripts/generate_certs.sh first).
VITE_HTTPS=false
